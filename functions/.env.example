# Production Functions always use the stable default Firestore database.
# A named ID is accepted only while FUNCTIONS_EMULATOR=true.
FIRESTORE_DATABASE_ID=(default)

# Production always enforces App Check in code. This flag can disable it only
# inside the local Functions emulator when no debug token is available.
ENFORCE_APP_CHECK=true
