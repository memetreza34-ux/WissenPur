{
  "name": "wissenpur-functions",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "lib/entry.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check:content": "tsx scripts/check-content-boundary.ts",
    "sync:questions": "tsx scripts/sync-question-bank.ts",
    "prepare:questions": "npm run check:content && npm run sync:questions",
    "build": "npm run prepare:questions && tsc",
    "lint": "npm run prepare:questions && tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only auth,functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsx": "^4.21.0",
    "typescript": "~6.0.3"
  }
}
